from setuptools import setup

setup(name='gym_myFetchPush',
      version='0.0.1',
      install_requires=['gym', 'numpy', 'matplotlib', 'dotmap','tqdm','future','termiplot']  # And any other dependencies foo needs
)
